# GPT-

세부 영어캠프 랜딩 페이지 기획 및 프로토타입 파일을 포함합니다.

## 실행 방법
```bash
python -m http.server --directory site 8000
```
이후 브라우저에서 `http://localhost:8000` 접속.

## 카카오채널 링크 설정
`site/app.js`의 `KAKAO_CHANNEL_URL` 값을 실제 카카오채널 링크로 변경하세요.
